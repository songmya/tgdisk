# =========================================================
# TGDrive 环境配置（复制为 .env 后填好真实值）
# =========================================================

# ===== 必填 =====

# Bot Token（向 @BotFather 申请；格式通常为 数字:字符串）
BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# 管理员 Telegram ID（多个用逗号分隔；获取：给 @userinfobot 发消息）
ADMIN_IDS=YOUR_TELEGRAM_ID

# ===== 代理 =====
# 国内机器访问官方 api.telegram.org 通常需要代理。
# 如果代理跑在宿主机 127.0.0.1，容器内访问不到 127.0.0.1；请改用宿主机 LAN IP，
# 或在 docker-compose.yml 中使用 network_mode: host。
# PROXY=http://user:pass@host:port
# PROXY=socks5://user:pass@host:port

# ===== 可选：自建 Bot API Server（减少 Bot API 50MB/20MB 限制影响）=====
# 启用 LOCAL_API_MODE 后，本地缓存文件会优先走 Bot API local mode 文件对象流式上传，
# 避免 Python 进程把大文件按大分片/整体读进内存。
# 注意：telegram-bot-api 必须以 --local/TELEGRAM_LOCAL=1 运行。
# LOCAL_API_BASE=http://botapi:8081
# LOCAL_API_MODE=true
# local mode 下载时 getFile 会返回 botapi 容器内路径；tgdisk 需挂载同一 botapi 数据目录。
# 如果两个容器内都挂到 /var/lib/telegram-bot-api，下面保持默认即可。
BOT_API_SERVER_DIR=/var/lib/telegram-bot-api
BOT_API_LOCAL_DIR=/var/lib/telegram-bot-api


# ===== Web/API 安全 =====
# 生产环境强烈建议设置。设置后 WebUI /api 与下载端点需要：
#   Authorization: Bearer <WEBUI_TOKEN>
# 或 URL query：?token=<WEBUI_TOKEN>
WEBUI_TOKEN=
# 逗号分隔 CORS 来源；留空时如果 WEBUI_TOKEN 已设置则默认不允许跨域。
CORS_ALLOW_ORIGINS=
# 续传 source_path 允许读取的服务器本地目录，逗号分隔；留空默认允许 UPLOAD_CACHE_DIR 和系统临时目录。
RESUME_ALLOWED_DIRS=

# WebDAV Basic/Digest Auth。生产环境建议设置；留空保持匿名兼容旧部署。
WEBDAV_USERNAME=
WEBDAV_PASSWORD=

# ===== 数据 =====
DB_PATH=data/tgdrive.sqlite3

# ===== 上传策略 =====
# 应用层最大文件大小（MB）。0 = 不限。
MAX_FILE_SIZE=0

# Telegram 上传分片大小（MB）。官方 Bot API 建议保持默认 18；自建 Bot API 且未启用
# LOCAL_API_MODE 文件流直传时可调大。启用 LOCAL_API_MODE 后，本地缓存文件上传不再依赖这里的分片大小。
TG_UPLOAD_CHUNK_SIZE_MB=1500

# 小于等于该阈值（MB）的文件走单次 sendDocument；默认等于 TG_UPLOAD_CHUNK_SIZE_MB。
# 启用 LOCAL_API_MODE 后，本地缓存文件上传不再需要用大阈值换少分片。
TG_SINGLE_UPLOAD_THRESHOLD_MB=1500

# 自建 Bot API 智能上传：≤1500MB 单文件，>1500MB 按 1500MB 分片。
TG_LOCAL_SINGLE_UPLOAD_LIMIT_MB=1500
TG_LOCAL_PART_SIZE_MB=1500

# 并发上传 Telegram 分片数。旧分片模式峰值内存约 UPLOAD_CONCURRENCY × TG_UPLOAD_CHUNK_SIZE_MB。
# 启用 LOCAL_API_MODE 后，本地缓存文件走文件流直传，内存占用主要由 Web/HTTP 框架缓冲决定。
UPLOAD_CONCURRENCY=1

# ===== WebUI 上传缓存 =====
# WebUI 上传先写入缓存，再后台上传 Telegram；可显示双阶段进度。
UPLOAD_CACHE_ENABLED=true
UPLOAD_CACHE_DIR=data/cache

# 缓存总容量上限（MB）。超限会拒绝新任务，并清理过期/已完成任务。
UPLOAD_CACHE_MAX_SIZE_MB=10240

# 单个缓存文件上限（MB）。0 = 不限制。
UPLOAD_CACHE_MAX_FILE_SIZE_MB=0

# 失败/取消/未完成缓存保留时间（小时）。
UPLOAD_CACHE_TTL_HOURS=24

# Telegram 上传完成后是否保留缓存文件。默认 false，完成即删除缓存文件。
UPLOAD_CACHE_KEEP_AFTER_DONE=false

# 浏览器上传到服务器缓存时的分片大小（字节），默认 8MiB。
BROWSER_CHUNK_SIZE=8388608

# ===== 杂项 =====
PAGE_SIZE=20
LOG_LEVEL=INFO
